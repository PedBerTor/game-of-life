plugins {
    id 'java'
}

group = 'org.gol'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // TODO: Include Gson latest version (https://mvnrepository.com/artifact/com.google.code.gson/gson)
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}
